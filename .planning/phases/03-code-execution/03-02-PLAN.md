---
phase: 03-code-execution
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - components/problem-detail.tsx
  - components/code-output.tsx
  - components/test-case-list.tsx
autonomous: true
requirements:
  - EXEC-01
  - EXEC-02
  - EXEC-03
  - EXEC-06

must_haves:
  truths:
    - "User can write code in Monaco editor"
    - "User can switch between languages (Python, JavaScript, Java, C++, Go)"
    - "User can enter custom input below editor"
    - "User can run code and see output in output panel"
    - "User sees execution errors in output panel"
  artifacts:
    - path: "components/code-output.tsx"
      provides: "Output panel showing stdout, stderr, execution time"
      min_lines: 40
    - path: "components/problem-detail.tsx"
      provides: "Updated with custom input area and Run Code button functionality"
      modifies: "Run Code button, adds input area, adds output panel"
  key_links:
    - from: "ProblemDetail Run button"
      to: "/api/execute"
      via: "handleRunCode function with fetch"
      pattern: "handleRunCode.*fetch.*api/execute"
    - from: "CodeEditor onChange"
      to: "code state"
      via: "setCode state setter"
      pattern: "onChange.*setCode"
---

<objective>
Implement "Run Code" functionality with custom input and output panel

Purpose: Allow users to execute their code with custom input and see the output. This is the primary workflow for testing code during development.
Output: Updated ProblemDetail with custom input area, output panel, and Run Code button
</objective>

<context>
@components/code-editor.tsx
@components/problem-detail.tsx
@types/index.ts

# Run Flow (from context)
- Custom input below editor
- Output panel below custom input
- Run button triggers execution
- Show loading state during execution
- Display stdout/stderr in output panel
- Display execution time after completion
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CodeOutput component</name>
  <files>components/code-output.tsx</files>
  <action>
Create components/code-output.tsx:
- Props: { stdout: string, stderr: string, time: string, memory: number, status: { id: number, description: string }, isLoading: boolean }
- Display states: loading (spinner), success (output), error (stderr in red)
- Show stdout in monospace font with bg-slate-900
- Show stderr in red text below stdout if present
- Show execution info (time, memory) in muted text below output
- Use Lucide icons: Play (running), CheckCircle (success), XCircle (error)

Structure:
```
- Header with status icon and description
- Output area (scrollable, max-height 200px)
- Footer with time and memory
```
  </action>
  <verify>
    <automated>Component renders without errors</automated>
  </verify>
  <done>CodeOutput displays execution results with proper styling</done>
</task>

<task type="auto">
  <name>Task 2: Update ProblemDetail with Run functionality</name>
  <files>components/problem-detail.tsx</files>
  <action>
Update ProblemDetail.tsx:
- Add state: customInput (string), output (ExecuteResponse | null), isRunning (boolean)
- Add custom input textarea below CodeEditor (height 80px, monospace font)
- Update "Run Code" button:
  - onClick calls handleRunCode
  - Shows loading spinner when isRunning
  - Disabled when isRunning
- handleRunCode function:
  - POST to /api/execute with { source: code, language: selectedLanguage, stdin: customInput }
  - Set output state with response
  - Handle errors with toast notification
- Add CodeOutput component below custom input:
  - Only show when output exists or isLoading
  - Pass output state and isRunning props
- Handle keyboard shortcut: Cmd+Enter (or Ctrl+Enter on Windows) triggers Run

Layout after update:
```
[Language Selector]
[Code Editor]
[Custom Input Textarea]
[Run Code Button] [Submit Button]
[Output Panel]
```
  </action>
  <verify>
    <automated>npm run build succeeds without errors</automated>
  </verify>
  <done>Run Code button executes code and displays output below editor</done>
</task>

</tasks>

<verification>
- [ ] Custom input textarea appears below editor
- [ ] Clicking Run Code sends code to /api/execute
- [ ] Output panel shows stdout/stderr results
- [ ] Execution time displayed in output panel
- [ ] Cmd+Enter triggers Run Code
</verification>

<success_criteria>
User can write code, enter custom input, click Run Code, and see execution output with time/memory. Errors displayed clearly in output panel.
</success_criteria>

<output>
After completion, create `.planning/phases/03-code-execution/03-02-SUMMARY.md`
</output>
