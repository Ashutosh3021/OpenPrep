---
phase: 03-code-execution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/judge0.ts
  - types/index.ts
  - app/api/execute/route.ts
  - app/api/submit/route.ts
autonomous: true
requirements:
  - EXEC-03
  - EXEC-04
  - EXEC-05
  - EXEC-06
  - EXEC-07

must_haves:
  truths:
    - "Backend can receive code from frontend and send to Judge0"
    - "Backend receives execution results and returns to frontend"
    - "Frontend has TypeScript types for execution responses"
  artifacts:
    - path: "lib/judge0.ts"
      provides: "Judge0 API client with language mapping"
      exports: ["executeCode", "submitCode", "LanguageId", "StatusId"]
    - path: "types/index.ts"
      provides: "TypeScript types for execution requests/responses"
      exports: ["ExecuteRequest", "ExecuteResponse", "SubmitRequest", "SubmitResponse"]
    - path: "app/api/execute/route.ts"
      provides: "POST endpoint for running code with custom input"
      exports: ["POST"]
    - path: "app/api/submit/route.ts"
      provides: "POST endpoint for submitting code against test cases"
      exports: ["POST"]
  key_links:
    - from: "components/problem-detail.tsx"
      to: "/api/execute"
      via: "fetch with POST body"
      pattern: "fetch.*api/execute"
    - from: "components/problem-detail.tsx"
      to: "/api/submit"
      via: "fetch with POST body"
      pattern: "fetch.*api/submit"
---

<objective>
Create backend API integration with Judge0 for code execution

Purpose: Provide the backend infrastructure to execute user code against Judge0 API. This is the foundation for both Run and Submit functionality.
Output: Two API routes (execute, submit) with Judge0 client and TypeScript types
</objective>

<context>
@components/code-editor.tsx
@components/problem-detail.tsx
@types/index.ts

# Judge0 Configuration (from research)
- Base URL: https://judge0-ce.p.rapidapi.com (or self-hosted)
- Languages: Python=71, JavaScript=63, Java=62, C++=54, Go=60
- Status codes: 3=Accepted, 4=Wrong Answer, 5=TLE, 6=Runtime Error, 7=MLE, 11=Compilation Error
- Run: single input, immediate result
- Submit: multiple test cases, need to poll for results
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Judge0 client library</name>
  <files>lib/judge0.ts</files>
  <action>
Create lib/judge0.ts with:
- Language ID mapping (python→71, javascript→63, java→62, cpp→54, go→60)
- Status code mapping (3→Accepted, 4→Wrong Answer, 5→TLE, 6→Runtime Error, 7→MLE, 11→Compilation Error)
- executeCode(source, languageId, stdin) function - POST to Judge0 /submissions
- submitCode(source, languageId, problemTestCases) function - handles multiple test cases
- Poll logic for getting submission results
- Error handling for network failures

Note: Use environment variable JUDGE0_API_URL and JUDGE0_API_KEY (if using RapidAPI) or configure for self-hosted Judge0.
  </action>
  <verify>
    <automated>Typescript compiles without errors - npx tsc --noEmit</automated>
  </verify>
  <done>lib/judge0.ts exports executeCode and submitCode functions with proper typing</done>
</task>

<task type="auto">
  <name>Task 2: Add execution types to TypeScript definitions</name>
  <files>types/index.ts</files>
  <action>
Add to types/index.ts:
- ExecuteRequest: { source: string, language: string, stdin?: string }
- ExecuteResponse: { stdout: string, stderr: string, status: { id: number, description: string }, time: string, memory: number }
- SubmitRequest: { source: string, language: string, problemId: number, testCases: Array<{ input: string, expectedOutput: string }> }
- SubmitResponse: { status: { id: number, description: string }, time: string, memory: number, testResults: Array<{ passed: boolean, input: string, expectedOutput: string, actualOutput: string }> }
- Language type: 'python' | 'javascript' | 'java' | 'cpp' | 'golang'
  </action>
  <verify>
    <automated>Typescript compiles - types are exported and usable</automated>
  </verify>
  <done>Types are exported from types/index.ts</done>
</task>

<task type="auto">
  <name>Task 3: Create POST /api/execute endpoint</name>
  <files>app/api/execute/route.ts</files>
  <action>
Create app/api/execute/route.ts:
- POST handler accepting { source, language, stdin }
- Validate source is non-empty string
- Call judge0.executeCode with language mapped to Judge0 ID
- Return ExecuteResponse with stdout, stderr, status, time, memory
- Handle errors and return appropriate error responses

Environment: Use process.env.JUDGE0_API_URL and process.env.JUDGE0_API_KEY
  </action>
  <verify>
    <automated>Route file exists and can be imported - no TypeScript errors</automated>
  </verify>
  <done>POST /api/execute accepts code and returns execution results</done>
</task>

<task type="auto">
  <name>Task 4: Create POST /api/submit endpoint</name>
  <files>app/api/submit/route.ts</files>
  <action>
Create app/api/submit/route.ts:
- POST handler accepting { source, language, problemId, testCases }
- testCases is array of { input, expectedOutput }
- For each test case, call judge0.executeCode sequentially or in parallel (handle rate limits)
- Build testResults array with passed/actualOutput for each case
- Return SubmitResponse with overall status, time, memory, and testResults
- Calculate overall status: if all passed → Accepted (3), else Wrong Answer (4)

Note: For submission, should compile first then run against all test cases.
  </action>
  <verify>
    <automated>Route file exists and can be imported - no TypeScript errors</automated>
  </verify>
  <done>POST /api/submit accepts code and test cases, returns aggregated results</done>
</task>

</tasks>

<verification>
- [ ] lib/judge0.ts exports executeCode and submitCode
- [ ] types/index.ts has ExecuteRequest, ExecuteResponse, SubmitRequest, SubmitResponse
- [ ] POST /api/execute returns execution results
- [ ] POST /api/submit returns test case results with pass/fail status
</verification>

<success_criteria>
Backend API ready to handle code execution requests. Frontend can send code to /api/execute and /api/submit and receive results with status, time, and memory.
</success_criteria>

<output>
After completion, create `.planning/phases/03-code-execution/03-01-SUMMARY.md`
</output>
