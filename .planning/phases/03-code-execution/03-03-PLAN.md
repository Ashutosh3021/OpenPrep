---
phase: 03-code-execution
plan: 03
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
files_modified:
  - components/problem-detail.tsx
  - components/submission-panel.tsx
autonomous: true
requirements:
  - EXEC-04
  - EXEC-05
  - EXEC-06
  - EXEC-07

must_haves:
  truths:
    - "User can submit code against test cases"
    - "User sees verdict (Accepted/Wrong Answer/TLE/Runtime Error/Compilation Error)"
    - "User sees time and space (memory) complexity"
    - "User sees which test cases passed/failed"
    - "Compilation errors shown inline (not in banner)"
  artifacts:
    - path: "components/submission-panel.tsx"
      provides: "Side panel showing submission results with verdict, complexity, test case details"
      min_lines: 60
    - path: "components/problem-detail.tsx"
      provides: "Updated with Submit button, side panel integration, error banner"
      modifies: "Submit button triggers submission, opens side panel on results"
  key_links:
    - from: "ProblemDetail Submit button"
      to: "/api/submit"
      via: "handleSubmitCode function with fetch"
      pattern: "handleSubmitCode.*fetch.*api/submit"
    - from: "submission-panel.tsx"
      to: "testResults state"
      via: "passed to component as props"
      pattern: "testResults.*passed"
---

<objective>
Implement "Submit Code" functionality with side panel results

Purpose: Allow users to submit their code against all test cases and see detailed results including verdict, time/memory, and which specific test cases passed or failed.
Output: Updated ProblemDetail with Submit button and SubmissionPanel side panel
</objective>

<context>
@components/problem-detail.tsx
@types/index.ts

# Submit Flow (from context)
- Side panel slides in from right (or opens as modal on mobile)
- Shows verdict with color coding: green (Accepted), yellow (Wrong Answer), red (TLE/Runtime Error)
- Shows time and space complexity
- Lists all test cases with pass/fail status
- Syntax/compilation errors shown inline in output panel (status 11)
- Keyboard shortcut: Cmd+Shift+Enter to submit

# Verdict Display
- Accepted (status 3): All green checkmarks
- Wrong Answer (status 4): Red X, shows expected vs actual
- TLE (status 5): Orange clock icon, "Time Limit Exceeded"
- Runtime Error (status 6): Red, shows error message
- MLE (status 7): Orange, "Memory Limit Exceeded"
- Compilation Error (status 11): Shown inline in output panel
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SubmissionPanel component</name>
  <files>components/submission-panel.tsx</files>
  <action>
Create components/submission-panel.tsx:
- Props: { 
    isOpen: boolean, 
    onClose: () => void, 
    status: { id: number, description: string },
    time: string,
    memory: number,
    testResults: Array<{ passed: boolean, input: string, expectedOutput: string, actualOutput: string }>,
    isSubmitting: boolean
  }
- Layout: Fixed side panel (width 400px on desktop, full screen on mobile)
- Header: Verdict icon + description with color coding
- Body:
  - Execution stats: Time (e.g., "0.02s"), Memory (e.g., "12MB")
  - Test case list: Each shows index, pass/fail icon, expand to see input/expected/actual
- Footer: "Run Code" button to test again, "Close" button
- States:
  - Loading: "Running test cases..." with spinner
  - Complete: Show verdict and results
  - Error: Show compilation/runtime error message

Use colors:
- Accepted: text-green-500, bg-green-500/10
- Wrong Answer: text-yellow-500, bg-yellow-500/10
- TLE/Runtime Error: text-red-500, bg-red-500/10
- Compilation Error: text-red-500, bg-red-500/10 with full error message

Animation: Slide in from right (transform + transition)
  </action>
  <verify>
    <automated>Component renders without errors</automated>
  </verify>
  <done>SubmissionPanel displays verdict, complexity, and test case details</done>
</task>

<task type="auto">
  <name>Task 2: Update ProblemDetail with Submit functionality</name>
  <files>components/problem-detail.tsx</files>
  <action>
Update ProblemDetail.tsx:
- Add state: 
  - submissionResults (SubmitResponse | null)
  - isSubmitting (boolean)
  - showSubmissionPanel (boolean)
- Update "Submit" button:
  - onClick calls handleSubmitCode
  - Shows loading spinner when isSubmitting
  - Opens submission panel when results arrive
- handleSubmitCode function:
  - POST to /api/submit with { source: code, language: selectedLanguage, problemId, testCases }
  - Get test cases from mockTestCases (for now)
  - Set submissionResults when response arrives
  - Set showSubmissionPanel to true
  - Handle errors with toast notification
- Handle compilation errors (status 11) inline in the output panel (not banner)
- Add SubmissionPanel component:
  - Render conditionally when showSubmissionPanel is true
  - Pass submissionResults and isSubmitting
- Handle keyboard shortcut: Cmd+Shift+Enter (or Ctrl+Shift+Enter on Windows) triggers Submit
- Close panel when clicking outside or pressing Escape

Layout change:
```
- Split view: Problem description (left), Editor (right)
- Editor panel: [Language Selector] [Code Editor] [Custom Input] [Run] [Submit]
- Side panel: Slides in on submit showing results
```
  </action>
  <verify>
    <automated>npm run build succeeds without errors</automated>
  </verify>
  <done>Submit button executes code against test cases and shows results in side panel</done>
</task>

</tasks>

<verification>
- [ ] Submit button triggers submission to /api/submit
- [ ] Side panel shows verdict with correct color coding
- [ ] Time and memory displayed in side panel
- [ ] Each test case shows pass/fail status
- [ ] Failed test cases show expected vs actual output
- [ ] Compilation errors show inline in output panel
- [ ] Cmd+Shift+Enter triggers Submit
</verification>

<success_criteria>
User can click Submit (or Cmd+Shift+Enter) to run code against all test cases and see detailed results in a side panel with verdict, complexity metrics, and test case breakdown.
</success_criteria>

<output>
After completion, create `.planning/phases/03-code-execution/03-03-SUMMARY.md`
</output>
