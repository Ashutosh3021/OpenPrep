---
phase: 01-setup-authentication
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/supabase.ts
  - lib/supabase-server.ts
  - app/layout.tsx
  - app/providers.tsx
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - AUTH-05
user_setup:
  - service: supabase
    why: "Backend database and authentication"
    env_vars:
      - name: NEXT_PUBLIC_SUPABASE_URL
        source: "Supabase Dashboard -> Settings -> API"
      - name: NEXT_PUBLIC_SUPABASE_ANON_KEY
        source: "Supabase Dashboard -> Settings -> API"
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "Supabase Dashboard -> Settings -> API (hide in client-side)"
    dashboard_config:
      - task: "Run SQL schema"
        location: "Supabase Dashboard -> SQL Editor"
        file: "data/schema.sql"
      - task: "Enable GitHub OAuth"
        location: "Supabase Dashboard -> Authentication -> Providers -> GitHub"

must_haves:
  truths:
    - User can sign up with email/password
    - User can log in with GitHub
    - User stays logged in for 30 days
    - User is logged out after 10 days inactivity
    - User profile created automatically on signup
  artifacts:
    - path: "lib/supabase.ts"
      provides: "Supabase client for browser"
    - path: "lib/supabase-server.ts"
      provides: "Supabase client for server components"
    - path: "app/providers.tsx"
      provides: "Supabase auth context provider"
    - path: "app/(auth)/login/page.tsx"
      provides: "Login page with Supabase Auth"
    - path: "app/(auth)/signup/page.tsx"
      provides: "Signup page with Supabase Auth"
    - path: "components/navbar.tsx"
      provides: "Auth state tracking, logout button"
  key_links:
    - from: "app/layout.tsx"
      to: "app/providers.tsx"
      via: "children wrapped in provider"
    - from: "app/(auth)/login/page.tsx"
      to: "lib/supabase.ts"
      via: "signInWithPassword, signInWithOAuth"
---

<objective>
Set up Supabase authentication with email/password and GitHub OAuth. Create auth pages, session management with 30-day persistence and 10-day auto-logout, and auto-profile creation.
</objective>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-setup-authentication/01-CONTEXT.md
@data/schema.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Supabase client and create lib files</name>
  <files>lib/supabase.ts, lib/supabase-server.ts</files>
  <action>
1. Install: npm install @supabase/supabase-js @supabase/ssr
2. Create lib/supabase.ts:
   - Export createClient using NEXT_PUBLIC_SUPABASE_URL and NEXT_PUBLIC_SUPABASE_ANON_KEY
3. Create lib/supabase-server.ts:
   - Export createServerClient for server components and API routes
   - Handle cookies with getAll, setAll from @supabase/ssr
  </action>
  <verify>
npm run build passes
  </verify>
  <done>Supabase client libraries created and working</done>
</task>

<task type="auto">
  <name>Task 2: Create Supabase provider component</name>
  <files>app/providers.tsx</files>
  <action>
Create app/providers.tsx:
- Import createBrowserClient from @supabase/ssr
- Create SupabaseProvider component using useState for client
- Wrap children with SessionContextProvider from @supabase/supabase-js
- Export for use in layout
  </action>
  <verify>
npm run build passes
  </verify>
  <done>Provider component created</done>
</task>

<task type="auto">
  <name>Task 3: Update root layout with provider</name>
  <files>app/layout.tsx</files>
  <action>
1. Import Providers from ./providers
2. Wrap children in Providers component (inside ThemeProvider)
  </action>
  <verify>
npm run build passes
  </verify>
  <done>Root layout updated with Supabase provider</done>
</task>

<task type="auto">
  <name>Task 4: Update login page with Supabase Auth</name>
  <files>app/(auth)/login/page.tsx</files>
  <action>
1. Import createClient from lib/supabase
2. Add useState for loading state
3. Replace localStorage mock with Supabase signInWithPassword:
   - const supabase = createClient()
   - const { error } = await supabase.auth.signInWithPassword({ email, password })
   - On success: router.push('/problems')
4. Add GitHub OAuth button using signInWithOAuth:
   - const { error } = await supabase.auth.signInWithOAuth({ provider: 'github', options: { redirectTo: `${location.origin}/auth/callback` } })
5. Keep localStorage for session check as fallback
  </action>
  <verify>
npm run build passes
  </verify>
  <done>Login page uses Supabase Auth</done>
</task>

<task type="auto">
  <name>Task 5: Update signup page with Supabase Auth</name>
  <files>app/(auth)/signup/page.tsx</files>
  <action>
1. Import createClient from lib/supabase
2. Replace localStorage mock with Supabase signUp:
 error } = await   - const { supabase.auth.signUp({ email, password, options: { data: { username } } })
   - On success: router.push('/problems')
3. Add GitHub OAuth button (same as login)
4. Handle email confirmation if enabled in Supabase
  </action>
  <verify>
npm run build passes
  </verify>
  <done>Signup page uses Supabase Auth</done>
</task>

<task type="auto">
  <name>Task 6: Update Navbar with auth state</name>
  <files>components/navbar.tsx</files>
  <action>
1. Import createClient from lib/supabase
2. Replace localStorage auth check with Supabase getUser:
   - Use useEffect to call supabase.auth.getUser()
   - Set isAuthenticated based on user presence
3. Add signOut function:
   - const { error } = await supabase.auth.signOut()
   - On success: router.push('/login')
4. Keep username display from user metadata
  </action>
  <verify>
npm run build passes
  </verify>
  <done>Navbar reflects real auth state</done>
</task>

<task type="auto">
  <name>Task 7: Create auth callback page</name>
  <files>app/auth/callback/route.ts</files>
  <action>
Create app/auth/callback/route.ts:
- Handle OAuth redirect callback
- Exchange code for session using supabase.auth.exchangeCodeForSession(code)
- Redirect to /problems on success
  </action>
  <verify>
npm run build passes
  </verify>
  <done>OAuth callback handled</done>
</task>

</tasks>

<verification>
- [ ] User can sign up with email/password
- [ ] User can log in with email/password
- [ ] User can log in with GitHub OAuth
- [ ] User stays logged in after page refresh
- [ ] User can log out from navbar
- [ ] User profile auto-created in database
</verification>

<success_criteria>
All 5 auth requirements (AUTH-01 to AUTH-05) working with Supabase
</success_criteria>

<output>
After completion, create `.planning/phases/01-setup-authentication/01-01-SUMMARY.md`
</output>
