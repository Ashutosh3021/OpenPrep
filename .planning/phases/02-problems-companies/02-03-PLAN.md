---
phase: 02-problems-companies
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
files_modified:
  - app/problems/[id]/page.tsx
  - components/problem-detail.tsx
autonomous: true
requirements:
  - PROB-05

must_haves:
  truths:
    - User can view problem detail with markdown description
    - User can view sample test cases below description
    - User can copy test case input
    - User can toggle output visibility
  artifacts:
    - path: "components/problem-detail.tsx"
      provides: "Problem detail with markdown and test cases"
  key_links:
    - from: "app/problems/[id]/page.tsx"
      to: "components/problem-detail.tsx"
      via: "import and render with problemId"
    - from: "problem-detail.tsx"
      to: "components/markdown-renderer.tsx"
      via: "import and use for description"
    - from: "problem-detail.tsx"
      to: "components/test-case-list.tsx"
      via: "import and render below description"
---

<objective>
Update problem detail page to use MarkdownRenderer and TestCaseList components. Integrate with problem detail view.
</objective>

<context>
@.planning/phases/02-problems-companies/02-01-SUMMARY.md
@components/problem-detail.tsx
@components/markdown-renderer.tsx
@components/test-case-list.tsx
@app/problems/[id]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update ProblemDetail with markdown and test cases</name>
  <files>components/problem-detail.tsx</files>
<action>
Update components/problem-detail.tsx:
1. Import MarkdownRenderer from @/components/markdown-renderer
2. Import TestCaseList from @/components/test-case-list
3. Replace static description with MarkdownRenderer:
   - Pass problem.description (or mock markdown content)
4. Add TestCaseList below description:
   - Pass problem.testCases array
   - Render in "Examples" section

For now, use mock test data if not in database:
```typescript
const mockTestCases = [
  { id: 1, input: '[2,7,11,15], 9', output: '[0,1]', explanation: '...' },
  { id: 2, input: '[3,2,4], 6', output: '[1,2]' },
  { id: 3, input: '[3,3], 6', output: '[0,1]' }
]
```

Keep existing:
- Difficulty badge
- Stats (acceptance, submissions, likes)
- Code editor panel (will be enhanced in Phase 3)
- Navigation back to problems list
</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>ProblemDetail renders markdown description and test cases</done>
</task>

<task type="auto">
<name>Task 2: Update problems [id] page</name>
<files>app/problems/[id]/page.tsx</files>
<action>
Update app/problems/[id]/page.tsx:
- If server component: fetch problem by ID from Supabase
- Pass problemId to ProblemDetail component
- If problem not found: show 404 page

If using mock data temporarily:
- Find problem from mockProblems array by id
- Pass to ProblemDetail
- Add TODO comment for Supabase integration
</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Problems detail page fetches and passes problem data</done>
</task>

</tasks>

<verification>
- [ ] Problem detail shows markdown rendered description
- [ ] Test cases display below description
- [ ] Copy button works on test case input
- [ ] Output toggle shows/hides expected output
</verification>

<success_criteria>
Problem detail page complete with markdown rendering and test case display. All 10 requirements (PROB-01 to PROB-07, COMP-01 to COMP-03) addressed.
</success_criteria>

<output>
After completion, create `.planning/phases/02-problems-companies/02-03-SUMMARY.md`
</output>
