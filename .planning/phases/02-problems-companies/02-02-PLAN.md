---
phase: 02-problems-companies
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - components/company-sidebar.tsx
  - components/recency-filter.tsx
  - app/problems/page.tsx
  - components/problem-list.tsx
autonomous: true
requirements:
  - PROB-01
  - PROB-02
  - PROB-03
  - PROB-04
  - COMP-01
  - COMP-02

must_haves:
  truths:
    - User can see company list with logos in sidebar
    - User can search and filter companies
    - User can select multiple companies (OR logic)
    - User can filter by recency (30d/3mo/6mo/6mo+)
    - Problem list updates based on company and recency filters
  artifacts:
    - path: "components/company-sidebar.tsx"
      provides: "Company filter sidebar with search and selection"
    - path: "components/recency-filter.tsx"
      provides: "Recency dropdown (30d/3mo/6mo/6mo+)"
    - path: "app/problems/page.tsx"
      provides: "Problems page with sidebar integration"
  key_links:
    - from: "app/problems/page.tsx"
      to: "components/company-sidebar.tsx"
      via: "render CompanySidebar, pass selectedCompanies callback"
    - from: "app/problems/page.tsx"
      to: "components/problem-list.tsx"
      via: "pass filtered problems based on sidebar selection"
    - from: "company-sidebar.tsx"
      to: "lib/supabase/companies.ts"
      via: "fetchCompanies() to load company list"
---

<objective>
Create company sidebar with recency filter and integrate with problems page. Update ProblemList to accept filtered problems from sidebar.
</objective>

<context>
@.planning/phases/02-problems-companies/02-01-SUMMARY.md
@components/problem-list.tsx
@lib/supabase/companies.ts
@lib/supabase/problems.ts

Research patterns:
- CompanySidebar: left panel, alphabetical list, checkboxes, search box
- Recency dropdown inside sidebar
- Use @radix-ui/react-checkbox, @radix-ui/react-scroll-area, @radix-ui/react-select
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create RecencyFilter component</name>
  <files>components/recency-filter.tsx</files>
  <action>
Create components/recency-filter.tsx:
- Accept recency value and onChange callback
- Options: all, 30d, 3mo, 6mo, 6mo+
- Use @radix-ui/react-select (existing in project)
- Display labels: "All time", "Past Month", "Past 3 Months", "Past 6 Months", "6 Months+"
- Pass selected value to parent onChange
</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>RecencyFilter renders dropdown with all time options</done>
</task>

<task type="auto">
  <name>Task 2: Create CompanySidebar component</name>
  <files>components/company-sidebar.tsx</files>
  <action>
Create components/company-sidebar.tsx:
- Accept companies, selectedCompanies, onSelectionChange, recencyFilter, onRecencyChange props
- Use @radix-ui/react-checkbox for company selection
- Use @radix-ui/react-scroll-area for scrollable list
- Use Input from @/components/ui/input for search
- Use Search icon from lucide-react

Structure:
- Fixed width: w-72 (288px), border-r
- Top section: RecencyFilter dropdown
- Middle section: Search input with Search icon
- Bottom section: Scrollable company list

Company list rendering:
- Filter by search query (client-side)
- Show top 20 initially (per CONTEXT.md)
- Each item: Checkbox + Company logo + Name + Problem count
- Group/checkbox toggles selection (OR logic)
- Click on row toggles checkbox

Company logo: img tag with w-6 h-6 rounded object-cover
Problem count: text-xs text-muted-foreground
</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>CompanySidebar renders with search, recency, and company list</done>
</task>

<task type="auto">
  <name>Task 3: Update Problems page with sidebar integration</name>
  <files>app/problems/page.tsx</files>
<action>
Update app/problems/page.tsx:
- Import CompanySidebar and RecencyFilter
- Create filter state: searchQuery, difficulty, category, selectedCompanies, recency
- Fetch companies using fetchCompanies() from lib/supabase/companies.ts
- Fetch problems using fetchProblems() when filters change
- Layout: flex h-[calc(100vh-64px)]
- Left: CompanySidebar with companies and filter callbacks
- Right: ProblemList with filtered problems

State management:
- useState for selectedCompanies: string[]
- useState for recency: string
- useCallback for filter update functions
- useEffect to refetch problems when filters change
</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Problems page integrates sidebar with filter callbacks</done>
</task>

<task type="auto">
  <name>Task 4: Update ProblemList to accept filtered problems</name>
  <files>components/problem-list.tsx</files>
<action>
Update components/problem-list.tsx:
- Change from internal filtering to accept problems as prop
- Remove internal searchQuery, difficultyFilter, categoryFilter state
- Accept problems: Problem[] and filters: FilterState props
- Accept onFilterChange callback for search/difficulty/category updates
- Update ProblemRow to show frequency badge using FrequencyBadge (from Wave 1)
- Keep existing pagination logic

If Supabase not ready yet:
- Fallback to mockProblems.filter() using filters
- Mark with TODO comment for Supabase integration
</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>ProblemList accepts external problems and shows frequency badges</done>
</task>

</tasks>

<verification>
- [ ] CompanySidebar renders with company list and search
- [ ] RecencyFilter shows dropdown with 30d/3mo/6mo/6mo+ options
- [ ] Selecting companies filters the problem list
- [ ] Changing recency updates problem list
- [ ] ProblemList shows frequency badges
</verification>

<success_criteria>
Company sidebar integrated with problems page. User can filter by companies and recency. Problem list displays with frequency badges.
</success_criteria>

<output>
After completion, create `.planning/phases/02-problems-companies/02-02-SUMMARY.md`
</output>
