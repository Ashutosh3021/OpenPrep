---
phase: 02-problems-companies
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - components/frequency-badge.tsx
  - components/test-case-list.tsx
  - components/markdown-renderer.tsx
  - lib/supabase/problems.ts
  - lib/supabase/companies.ts
  - types/index.ts
autonomous: true
requirements:
  - PROB-06
  - PROB-07
  - COMP-03

must_haves:
  truths:
    - User can see frequency score as progress bar badge
    - User can view test cases with copy button
    - User can view formatted markdown descriptions
  artifacts:
    - path: "components/frequency-badge.tsx"
      provides: "Progress bar badge for frequency score"
    - path: "components/test-case-list.tsx"
      provides: "Test case display with copy functionality"
    - path: "components/markdown-renderer.tsx"
      provides: "Rich markdown rendering with syntax highlighting"
    - path: "lib/supabase/problems.ts"
      provides: "Problem queries with filtering"
    - path: "lib/supabase/companies.ts"
      provides: "Company queries"
  key_links:
    - from: "components/markdown-renderer.tsx"
      to: "problem-detail.tsx"
      via: "import and use in description section"
    - from: "components/test-case-list.tsx"
      to: "problem-detail.tsx"
      via: "import and render below description"
---

<objective>
Install dependencies and create core UI components: frequency badge, test case list, and markdown renderer. Also create Supabase query helpers.
</objective>

<context>
@.planning/phases/02-problems-companies/02-CONTEXT.md
@.planning/phases/02-problems-companies/02-RESEARCH.md
@components/problem-list.tsx
@components/problem-detail.tsx
@lib/mock-data.ts

Libraries to install:
- react-markdown ^9.0.0
- remark-gfm ^4.0.0
- remark-math ^7.0.0
- rehype-katex ^7.0.0
- rehype-highlight ^7.0.0
- react-syntax-highlighter ^15.0.0
- katex ^0.16.0
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install markdown and syntax highlighting dependencies</name>
  <files>package.json</files>
  <action>
Install dependencies:
- npm install react-markdown remark-gfm remark-math rehype-katex rehype-highlight react-syntax-highlighter katex

Add to package.json dependencies. Verify installation with npm list react-markdown.
  </action>
  <verify>
    <automated>npm list react-markdown remark-gfm rehype-katex react-syntax-highlighter</automated>
  </verify>
  <done>All markdown dependencies installed successfully</done>
</task>

<task type="auto">
  <name>Task 2: Create FrequencyBadge component</name>
  <files>components/frequency-badge.tsx</files>
  <action>
Create components/frequency-badge.tsx:
- Import Progress from @/components/ui/progress
- Accept frequency (number 0-100) and maxFrequency props
- Calculate percentage with safety check for zero
- Render horizontal progress bar with accent color
- Display numeric frequency next to bar
- Use @radix-ui/react-progress (existing in project)
- Style: w-16 h-2 progress bar, text-xs text-muted-foreground for number
</action>
<verify>
    <automated>npm run build</automated>
  </verify>
  <done>FrequencyBadge component renders progress bar with percentage</done>
</task>

<task type="auto">
  <name>Task 3: Create TestCaseList component</name>
  <files>components/test-case-list.tsx</files>
  <action>
Create components/test-case-list.tsx:
- Accept testCases array prop with {id, input, output, explanation}
- Show first 3 test cases (PROB-06 requirement)
- Each test case: display "Example N" label
- Input displayed in pre/code block with bg-background
- Output hidden by default with toggle button (eye icon)
- Copy button using navigator.clipboard.writeText
- Show check icon briefly after copy
- Use lucide-react icons: Copy, Check, Eye, EyeOff
- Use Button component from @/components/ui/button
</action>
<verify>
    <automated>npm run build</automated>
  </verify>
  <done>TestCaseList shows examples with copy and toggle output</done>
</task>

<task type="auto">
  <name>Task 4: Create MarkdownRenderer component</name>
  <files>components/markdown-renderer.tsx</files>
  <action>
Create components/markdown-renderer.tsx:
- Accept content string prop
- Import ReactMarkdown, remark-gfm, remark-math, rehype-katex, rehype-highlight
- Import { Prism as SyntaxHighlighter } from react-syntax-highlighter
- Import oneDark style from react-syntax-highlighter/dist/cjs/styles/prism
- Configure remarkPlugins: [remarkGfm, remarkMath]
- Configure rehypePlugins: [rehypeKatex, rehypeHighlight]
- Custom code component: detect language from className (regex: /language-(\w+)/)
- Inline code: simple <code> with bg-card px-2 py-1 rounded
- Custom img component: max-w-full h-auto rounded-lg, loading="lazy"
- IMPORTANT: Import katex CSS at top: import 'katex/dist/katex.min.css'
- Wrap in div with prose prose-invert max-w-none
</action>
<verify>
    <automated>npm run build</automated>
  </verify>
  <done>MarkdownRenderer renders code blocks with syntax highlighting and math</done>
</task>

<task type="auto">
  <name>Task 5: Create Supabase query helpers</name>
  <files>lib/supabase/problems.ts, lib/supabase/companies.ts, types/index.ts</files>
  <action>
Create lib/supabase/problems.ts:
- fetchProblems(options): difficulty, searchQuery, tags, companyIds, recency
- Use supabase.from('problems').select() with joins
- Use .overlaps() for company_ids array filtering
- Add recency filtering using last_asked date column
- Export ProblemWithRelations type

Create lib/supabase/companies.ts:
- fetchCompanies(): select all companies ordered by name
- fetchCompanyById(id): get single company
- Export Company type

Create types/index.ts:
- Problem interface extending existing with company data
- Company interface with logo, name
- ProblemCompany junction type
- TestCase interface
</action>
<verify>
    <automated>npm run build</automated>
  </verify>
  <done>Supabase query helpers created with proper typing</done>
</task>

</tasks>

<verification>
- [ ] npm install succeeds with all markdown packages
- [ ] FrequencyBadge renders progress bar with percentage
- [ ] TestCaseList shows examples with copy functionality
- [ ] MarkdownRenderer renders code blocks and math equations
- [ ] Supabase query helpers have proper TypeScript types
</verification>

<success_criteria>
All core UI components created: FrequencyBadge, TestCaseList, MarkdownRenderer. Supabase queries ready for Wave 2 integration.
</success_criteria>

<output>
After completion, create `.planning/phases/02-problems-companies/02-01-SUMMARY.md`
</output>
